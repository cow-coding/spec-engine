{
  "compilerOptions": {
    "module": "commonjs",
    "target": "es6",
    "outDir": "out",
    "lib": [
      "ES6",
      "DOM"  // 👈 1. 여기에 "DOM"을 꼭 추가해야 fetch와 Headers 에러가 사라집니다.
    ],
    "sourceMap": true,
    "rootDir": "src",
    "strict": true,   
    "skipLibCheck": true, // 👈 2. 이 줄을 추가하면 Buffer 관련 에러가 사라집니다.
    
    // (기타 기존 설정들은 그대로 두셔도 됩니다)
    "moduleResolution": "node",
    "allowSyntheticDefaultImports": true
  },
  "exclude": [
    "node_modules",
    ".vscode-test"
  ]
}